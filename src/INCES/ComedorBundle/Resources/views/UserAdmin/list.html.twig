
<div id="my-col-text">

    <h1>Lista Usuarios Administradores</h1>

    <table class="bordered">
        <thead>
            <tr>
                <th>Nombre de Usuario</th>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Cédula</th>
                <th>Rol</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
            <tr>
                <td>{{ entity.username }}</td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.apellido }}</td>
                <td>{{ entity.cedula }}</td>
                {% for val in entity.roles %}
                    {% if loop.first %}
                        <td>{{ val }}</td>
                    {% endif %}
                {% endfor %}
                <td>
                    <a href="{{ app.request.uriForPath('/#!/admin/') }}{{ entity.id }}/show"><img src="{{ asset('img/show.ico') }}" height="30" width="30" /></a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <a class="mybutton" href="{{ app.request.uriForPath('/#!/admin/register') }}">
        Crear Nuevo
    </a>
    </ul>
</div> <!-- /col-text -->
