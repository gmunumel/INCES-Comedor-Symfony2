{# src/INCES/ComedorBundle/Resources/views/Menu/new.html.twig #}
{#
{% extends '::base.html.twig' %}

{% block body %}
#}
<h1>Menu creation</h1>

<form action="{{ path('menu_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('menu') }}">
            Back to the list
        </a>
    </li>
</ul>
{#
{% endblock %}
#}
